aliases:: command-line argument, command line argument, command line arguments

- Command line arguments are defined **as inputs provided to a command in UNIX** (or whichever system you're using) that **influence the actions the command performs**, typically **beginning with a hyphen (-)** and separated by spaces when multiple arguments are used.
- credit: https://www.sciencedirect.com/topics/computer-science/command-line-argument